name: test

# concurrency: prod

on:
#  push:
#    branches:
#      - main
      
  workflow_dispatch:

jobs:
  test-prod:
    runs-on: ubuntu-latest
    environment: new
    steps:
      - uses: actions/checkout@v3
      - name: test
        run: echo "prod deployed"
